---
title: "ggplot2"
date-modified: last-modified
date-format: iso
highlight-style: pygments
number-sections: true
format: 
  html:
    toc: true
    toc-depth: 3
    toc-location: left
    toc-title: Contents
    code-fold: show
editor: visual
execute:
  warning: false
  cache: true
crossref: 
  fig-title: 图 #图的默认标签（默认是"Figure"）
  fig-prefix: 图 # 图的默认引用样式（默认是"Figure"）
theme:
  light: flatly
  dark: darkly
---

# 字体设置

```{r}
#| include: false
library(showtext)
showtext_auto(enable=T)
font_add("STKaiti","/Library/Fonts/stkaiti.ttf")
```

```{r}
library(ggplot2)
mytheme <-   theme(
    text = element_text(size = 16,
                        family = "STKaiti"),
    axis.title = element_text(size = 20)
  )
```

# 准备

## 数据准备

```{r}
# 查看数据
data("BOD")
```

## 包准备

```{r}
#| message: false
library(ggplot2)
```

# 案例演示

## BOD data

```{r}
ggplot(data = BOD,
       mapping = aes(x = Time,y = demand))+
  geom_point(size=5)+
  geom_line(color="red")
```

```{r}
ggplot(BOD,aes(Time,demand))+
  geom_point(size = 3)+
  geom_line(color = "red")
```

## CO2数据集

### 散点图

```{r}
library(ggplot2)
ggplot(CO2,aes(conc,uptake,
             color = Treatment))+
  geom_point(size=3,alpha=0.5)+
  geom_smooth(method = "lm",formula = y~x,se=F)+
  facet_wrap(~Type)+
  labs(x="CO2浓度",y="CO2摄取量",title = "不同地区植物二氧化碳摄取量")+
  theme_bw()+mytheme
```

解决标注的上下标问题

```{r}
ggplot(CO2,aes(conc,uptake,
             color = Treatment))+
  labs(x=expression("CO"[2]~"浓度"),
       y=expression("CO" [2]~"摄取量(μmol/m"^2~"sec)"))+mytheme
```

### 箱线图

```{r}
ggplot(CO2,aes(Treatment,uptake))+
  geom_boxplot()+
  geom_point(aes(size = conc,
                 color = Plant),
             alpha = 0.5)+
  facet_wrap(~Type)+
  coord_flip()+
  theme_bw()+
  labs(title = "耐寒处理与非耐寒处理")+mytheme
```

设置图例标题为中文

```{r}
ggplot(CO2,aes(Treatment,uptake))+
  geom_boxplot()+
  scale_x_discrete(labels=c("耐寒","非耐寒"))+
  geom_point(aes(size = conc,color=Plant))+
  guides(size=guide_legend(title = "浓度"),color=guide_legend(title = "植物"))+
  theme(text = element_text(family = "STKaiti"))+
  theme_bw()
```

## mpg

### 散点图

```{r}
ggplot(mpg,aes(displ,cty))+
  geom_point(aes(color=drv,size = trans),alpha=0.5)+
  geom_smooth(formula = y~x,method = "lm")+
  facet_wrap(~year,nrow = 1)+
  labs(x="发动机排量",y="城市地段每公里行驶的加仑数",title="汽车燃油效率散点图")+
  theme(text = element_text(family = "STKaiti"))
```

## mammals 62种陆地哺乳动物的体重(kg)和大脑重量(g)

### 散点图

```{r}
#| message: false
library(MASS)
```

```{r}
knitr::kable(mammals[1:15,])
```

```{r}
#| message: false
p1 <- ggplot(mammals,aes(x = body,y = brain))+
  geom_point(alpha=0.6)+
  geom_smooth(method = "lm")+
  labs(title = "陆地哺乳动物体重与大脑重量之间的关系")
```

```{r}
p1+ggrepel::geom_label_repel(
  aes(label=rownames(mammals)),size=1,max.overlaps = 100
)+mytheme
```

考虑到极端值对线性回归结果的影响，非洲象和亚洲象的影响，对两个变量同时进行对数转换

```{r}
#| message: false
ggplot(mammals,aes(x=body,y=brain))+
  geom_point(alpha=0.6,size=3)+
  coord_fixed(xlim = c(10^-3,10^4),ylim = c(10^-1,10^4))+
  scale_x_log10(
    expression("体重(log"["10"]*"(Kg))",
               breaks = tran_breaks("log10",function(x) 10^x),
               labels=trans_format("log10",math_format(10^.x)))
  )+
  scale_y_log10(
expression("平均脑重(log"["10"]*"(g))",
           breaks = tran_breaks("log10",function(x) 10^x),
           labels=trans_format("log10",math_format(10^.x)))
  )+
  geom_smooth(method = "lm",col="#C42126",se=FALSE,linewidth=1)+
  theme_classic()+mytheme
```

## iris鸢尾花数据集

### 数据(Data)

```{r}
knitr::kable(iris[1:15,])
```

### 视觉通道映射(Aes)

```{r}
ggplot(iris,aes(x=Sepal.Length,y=Sepal.Width))
```

### 几何对象(geom)

```{r}
ggplot(iris,aes(x=Sepal.Length,y=Sepal.Width))+geom_point()
```

### 分面(facet)

```{r}
ggplot(iris,aes(x=Sepal.Length,y=Sepal.Width))+geom_point()+
  facet_wrap(~Species)
```

### 统计(statistics)

```{r}
ggplot(iris,aes(x=Sepal.Length,y=Sepal.Width))+
  geom_point()+
  facet_wrap(~Species)+
  geom_smooth(method = "lm",se=F,color="red")
```

### 坐标系(coordinates)

```{r}
ggplot(iris,aes(x=Sepal.Length,y=Sepal.Width))+
  geom_point()+
  facet_wrap(~Species)+
  geom_smooth(method = "lm",se=F,color="red",linetype=2)+
  labs(x="花萼长度(cm)",y="花萼宽度(cm)",title = "鸢尾花不同品种的花萼长宽比较")+
  coord_cartesian(xlim = c(4,8),ylim = c(1.5,4.5))
```

### 主题(theme)

```{r}
ggplot(iris,aes(x=Sepal.Length,y=Sepal.Width))+
  geom_point()+
  facet_wrap(~Species)+
  geom_smooth(method = "lm",se=F,color="red")+
  labs(x="花萼长度(cm)",y="花萼宽度(cm)",title = "鸢尾花不同品种的花萼长宽比较")+
  coord_cartesian(xlim = c(4,8),ylim = c(1.5,4.5))+
  theme_bw()+
  theme(text = element_text(family = "STKaiti"))
```

# ggplot2作图的一般步骤

-   准备数据，一般为数据框， 且一般为长表，即每个观测时间占一行， 每个观测变量占一列。
-   将数据输入到ggplot()函数中， 并指定参与作图的每个变量分别映射到哪些图形特性， 比如映射为x坐标、y坐标、颜色、形状等。 这些映射称为aesthetic mappings或aesthetics。
-   选择一个合适的图形类型， 函数名以geom_开头， 如geom_point()表示散点图。 图形类型简称为geom。 将ggplot()部分与geom_xxx()部分用加号连接。 到此已经可以作图，下面的步骤是进一步的细化设定。
-   设定适当的坐标系统， 如coord_cartesian(), scale_x_log10()等。 仍用加号连接。
-   设定标题和图例位置等，如labs()。 仍用加号连接。

这个流程的大致模板为

```{r}
#| eval: false
p <- ggplot(data=<输入数据框>,
  mapping=aes(<维度>=<变量名>,
    <维度>=<变量名>, <...>))
p + geom_<图形类型>(<...>) + 
  scale_<映射>_<类型>(<...>) +
  coord_<类型>(<...>) +
  labs(<...>)
```

其中\<...\>表示额外的选项。 变量p包含做出的图形的所有数据与设定， 变量名可以任意取。

# 课本案例

## 准备数据

```{r}
load("../Output/chapter03绘图基础.RData")
```

## 绘制散点图

```{r}
ggplot(data2_1,aes(x=R语言,y=Python语言))+
  geom_point()
```
